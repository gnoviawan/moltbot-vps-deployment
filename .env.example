# Clawdbot Gateway Environment Variables
# Copy this file to .env and update with your values

# === Gateway Configuration ===
# Generate a strong random token for gateway authentication
# Command: openssl rand -hex 32
CLAWDBOT_GATEWAY_TOKEN=change-me-to-random-32-char-hex

# Gateway bind mode: loopback | lan | 0.0.0.0
# - loopback: 127.0.0.1 only (most secure, use with reverse proxy)
# - lan: 0.0.0.0 (allows LAN access)
# - 0.0.0.0: binds to all interfaces (public, not recommended without auth)
CLAWDBOT_GATEWAY_BIND=lan

# Gateway port (default: 18789)
CLAWDBOT_GATEWAY_PORT=18789

# === Persistence Paths (on host) ===
# These directories will be mounted from host to container
# Adjust paths based on your Dokploy setup
CLAWDBOT_CONFIG_DIR=./data/config
CLAWDBOT_WORKSPACE_DIR=./data/workspace

# === External Tool Secrets ===

# Gmail Keyring Password (for gog CLI)
# Required for Gmail/Calendar/Drive/Sheets integration
# Generate with: openssl rand -hex 32
GOG_KEYRING_PASSWORD=change-me-to-random-32-char-hex

# Optional: XDG Config Home (default: /home/node/.clawdbot)
XDG_CONFIG_HOME=/home/node/.clawdbot

# === Model Provider Credentials ===
# Uncomment and add your provider credentials if needed

# Anthropic API Key
# ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API Key
# OPENAI_API_KEY=sk-...

# Google AI API Key
# GOOGLE_API_KEY=AIza...

# Z.AI API Key
# ZAI_API_KEY=...

# === Channel Configuration ===
# Discord Bot Token (for Discord integration)
# DISCORD_TOKEN=MTAw...

# Telegram Bot Token (for Telegram integration)
# TELEGRAM_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# WhatsApp (for wacli integration)
# WhatsApp session will be stored in CLAWDBOT_CONFIG_DIR
# WACLI_ACCOUNT=you@example.com

# === Optional: Tailscale Configuration ===
# If you want to use Tailscale for secure remote access
# TAILSCALE_AUTH_KEY=tskey-auth-...
# TAILSCALE_STATE_DIR=/home/node/.tailscale

# === Optional: Resource Limits ===
# These are configured in docker-compose.yml
# Adjust based on your VPS resources
